// This is the config folder for ProjectFNF
// These are config options the player doesn't get to access so you can keep the game looking as intended.
public var CONFIGGfCar:Bool = true; // If you want GF on week 4 to be sitting on the car in the back and not on a speaker.
public var CONFIGTitle:Bool = true; // Title screen uses ProjectFNF logo instead of the Logo bumpin.
